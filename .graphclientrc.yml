sources:
  - name: coupon-finance
    handler:
      graphql:
        endpoint: '{context.url:https://dev-subgraph.coupon.finance/subgraphs/name/coupon-subgraph}'
    transforms:
      - autoPagination:
          validateSchema: true
          limitOfRecords: 100000

documents:
  - ./src/api/**/*.graphql


codegen:
  contextType: 'MeshContext & { url: string }'
